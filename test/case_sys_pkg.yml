- name: test system packages' commands
  shell: "command -v {{ item }} | wc -l "
  register: x
  failed_when: "'0' in x.stdout"
  with_items:
    - tar
    - iptables
    - docker
    - arping
    - inotifywait
    - gzip
    - openssl
    - expect
    - ethtool
    - ifconfig
    - unzip