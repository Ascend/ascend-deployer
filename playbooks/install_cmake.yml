- hosts: '{{ hosts_name }}'
  become: yes
  become_user: root
  tasks:
    - name: install cmake3.5.2 for BigCloud Linux 7.6
      import_tasks: task_cmake.yml
      when: os_and_arch == 'BigCloud_7.6_aarch64' or os_and_arch == 'BigCloud_7.6_x86_64'
