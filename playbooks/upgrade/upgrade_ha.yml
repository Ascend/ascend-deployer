- hosts: '{{ hosts_name }}'
  tasks:
    - name: upgrade ha
      import_tasks: task_ha.yml
      when: ansible_user_uid == 0

    - name: message
      debug:
        msg:
        - "not support upgrading ha by non-root user, please switch to root user"
      when: ansible_user_uid != 0
