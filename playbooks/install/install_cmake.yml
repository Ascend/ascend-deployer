- hosts: '{{ hosts_name }}'
  tasks:
    - name: get cmake version
      shell: cmake --version | awk -F" " '{print $3;exit}'
      register: cmake_ver

    - name: install cmake3.18.6
      import_tasks: task_cmake.yml
      when: cmake_ver.stdout is version("3.18.3", "<")
